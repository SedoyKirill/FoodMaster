"""Telegram-бот «Супостат» (@SoupoStatBot) — интерфейс к планировщику питания.

Раскладка (TZ-M7 §2): ``bot.py`` — сеть и очередь апдейтов; ``router.py`` —
личность, лимиты и выбор сцены; ``dispatch.py`` — команда или кнопка → сцена;
``scenes/`` — диалоги; ``render.py`` — тексты и клавиатуры; ``callbacks.py`` —
кодек кнопок; ``fsm.py`` — состояние формы; ``repository.py`` — свои запросы.
Всё, кроме ``bot.py``, тестируется без сети, Telegram и Postgres.
"""
