# ТЗ M4 — Профили семьи и нутрициология

## Цель
Детерминированная математика питания: нормы калорий/БЖУ по каждому члену семьи,
пересчёт сырое↔готовое (ужарка/уварка), КБЖУ порции. Никакого LLM — только
формулы и таблицы, покрытые тестами.

## Зависимости
M1 (таблицы `family_members`, `cook_coefficients`, `ingredients`).

## Состав работ

### 1. Нормы калорий (`app/nutrition/needs.py`)

Взрослые (≥18 лет) — формула Миффлина–Сан-Жеора:
```
BMR муж = 10*вес(кг) + 6.25*рост(см) − 5*возраст + 5
BMR жен = 10*вес(кг) + 6.25*рост(см) − 5*возраст − 161
TDEE = BMR * коэффициент активности:
  sedentary 1.2 | light 1.375 | moderate 1.55 | high 1.725
```

Дети и подростки (1–17 лет) — по возрастным нормам (Роспотребнадзор МР 2.3.1.0253-21),
зашить таблицей в код:
```
1–3 года: 1400 ккал | 4–6: 1800 | 7–10: 2100 (м)/1900 (ж — с 7 лет разделение)
11–13: 2500 (м)/2300 (ж) | 14–17: 2900 (м)/2500 (ж)
```
(Исполнителю: сверить точные значения с МР 2.3.1.0253-21 и указать источник в коде.)

- `kcal_override` в профиле бьёт расчёт (пользователь худеет/набирает —
  задал цель руками).
- Распределение БЖУ по умолчанию: белки 15–20%, жиры 30%, углеводы 50–55%
  калорийности; для детей белок считать также в г/кг веса (проверка минимума).
- Распределение по приёмам пищи: завтрак 25%, обед 35%, ужин 30%, перекус 10%
  (конфигурируемо на семью).
- `family_daily_needs(family_id) -> {member_id: Needs, total: Needs}`.

### 2. Коэффициенты термообработки (`data/cook_coefficients.csv` → БД)
Наполнить таблицу `cook_coefficients` по сборникам рецептур (справочник
Скурихина / сборник технологических нормативов). Ориентиры:

| Категория | Метод | weight_factor |
|---|---|---|
| мясо | fry | 0.63 |
| мясо | boil | 0.60 |
| мясо | bake/stew | 0.68 |
| птица | fry | 0.70 |
| рыба | fry | 0.80 |
| овощи | boil | 0.90 |
| овощи | fry | 0.75 |
| крупы | boil | 2.5 (разваривание: 100 г сухой → 250 г каши) |
| макароны | boil | 2.2 |

ВАЖНО про калории: КБЖУ считаем ВСЕГДА от сырого веса заложенных продуктов —
при ужарке калории не исчезают (уходит вода). Коэффициенты нужны для:
а) веса готовой порции («порция 320 г»), б) обратного пересчёта, когда
пользователь в дневнике M7 пишет «съел 200 г готовой гречки» → 80 г сухой →
КБЖУ. Исключение — жарка с маслом: добавлять +10 г масла (90 ккал) на порцию
жареного блюда, если масло не указано в рецепте явно.

### 3. Расчёт блюда (`app/nutrition/dish.py`)
- `dish_nutrition(recipe_id) -> per_serving {kcal, p, f, c, raw_g, cooked_g}`.
- `portion_for(member, recipe)` — множитель порции: норма приёма пищи члена
  семьи / калорийность стандартной порции (округлять до 0.25; ребёнку не
  больше 1.5 порции, взрослому не больше 2.0).
- Валидации: рецепт без КБЖУ хотя бы одного значимого (>5% веса) ингредиента
  помечается флагом `recipes.nutrition_incomplete` — планировщик M5 такие пропускает.

### 4. CRUD профилей
- REST: `POST/GET/PATCH /api/v1/families`, `/api/v1/members` (использует M6:
  страница «Семья» и мастер первого запуска).
- Вес: текущий — `family_members.weight_kg`, история — `weight_log` (пишет M7);
  новая запись веса обновляет текущий, нормы пересчитываются автоматически
  (считаются на лету от профиля).
- Поле `restrictions` (JSONB): `{"allergies": ["орехи"], "dislikes": ["рыба"],
  "diet": null|"vegetarian"|...}` — M5 обязан трактовать allergies как жёсткий
  запрет (SQL-фильтр), dislikes — как сильный штраф при подборе.

## Критерии готовности
- [ ] Юнит-тесты формул: мужчина 35 лет/80 кг/180 см/moderate → TDEE ≈ 2700±50;
      ребёнок 5 лет → 1800; переопределение работает.
- [ ] Тест ужарки: 400 г сырого мяса fry → 252 г готового; 100 г гречки →
      250 г каши; калории от способа готовки не меняются (кроме масла).
- [ ] `family_daily_needs` для семьи из 3 человек считается корректно
      (ручная сверка одного кейса).
- [ ] Обратный пересчёт готовое→сырое→КБЖУ работает (нужен M7).
